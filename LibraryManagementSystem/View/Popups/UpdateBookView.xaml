<Window x:Class="LibraryManagementSystem.View.Popups.UpdateBookView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryManagementSystem.View.Popups"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:converters="clr-namespace:LibraryManagementSystem.Converter"
        xmlns:dc="clr-namespace:LibraryManagementSystem.ViewModel"
        mc:Ignorable="d"
        Height="730" Width="500" WindowStartupLocation="CenterScreen" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Loaded="Window_Loaded">

    <Window.DataContext>
        <dc:BookViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <converters:BooleanConverter x:Key="InverseBooleanConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" CornerRadius="20 20 0 0" Background="#8B5DFF">
            <StackPanel>
                <TextBlock Text="Update Book" Style="{StaticResource pagetitle}" Margin="40 20 0 0" FontSize="20" Foreground="White"/>
                <Image Source="/Images/Header/close.png" Name="btn_close" Margin="0 -26 20 0" Style="{StaticResource closeMinimizeButton}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseUp">
                            <i:InvokeCommandAction Command="{Binding MouseUpCloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Image>
            </StackPanel>
        </Border>

        <Border Background="LightGray" Grid.Row="1">
            <Grid  Margin="40 40 40 10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <!--Search Book-->
                <Border Grid.Row="0" Margin="0 0 0 25">
                    <StackPanel Orientation="Horizontal">
                        <TextBox x:Name="TxtSearch" Text="{Binding CurrentBookInDisplay.BookID, Mode=TwoWay}" Padding="8" FontSize="15" Background="Transparent" Width="300" Height="40"/>
                        <Button Width="120" Content="Search ID" Style="{StaticResource UpdateSearchButton}" Command="{Binding SearchCommand}"/>
                    </StackPanel>
                </Border>
                
                <!--Book Title-->
                <Border Grid.Row="1" Margin="0 0 0 20">
                    <StackPanel>
                        <TextBlock Text="Book Title" Style="{StaticResource textblockname}"/>
                        <TextBox x:Name="TxtName" Text="{Binding Path=CurrentBookInDisplay.BookTitle, Mode=TwoWay}" FontSize="15" Background="LightGray" Style="{StaticResource textbox}"/>
                    </StackPanel>
                </Border>

                <!--Author-->
                <Border Grid.Row="2" Margin="0 20 0 20">
                    <StackPanel>
                        <TextBlock Text="Author" Style="{StaticResource textblockname}"/>
                        <TextBox x:Name="TxtPosition" Text="{Binding Path=CurrentBookInDisplay.BookAuthor, Mode=TwoWay}" FontSize="15" Background="LightGray" Style="{StaticResource textbox}"/>
                    </StackPanel>
                </Border>

                <!--Book ISBN-->
                <Border Grid.Row="3" Margin="0 20 0 20">
                    <StackPanel>
                        <TextBlock Text="ISBN" Style="{StaticResource textblockname}"/>
                        <TextBox x:Name="TxtEmail" Text="{Binding Path=CurrentBookInDisplay.BookISBN, Mode=TwoWay}" FontSize="15" Background="LightGray" Style="{StaticResource textbox}"/>
                    </StackPanel>
                </Border>

                <!--Book Category-->
                <Border Grid.Row="4" Margin="0 20 0 20">
                    <StackPanel>
                        <TextBlock Text="Category" Style="{StaticResource textblockname}"/>
                        <ComboBox  Margin="0 20 0 0" ItemsSource="{Binding Categories}"  DisplayMemberPath="name" SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"/>
                    </StackPanel>
                </Border>

                <!--Availability-->
                <Border Grid.Row="5" Margin="0 20 0 20">
                    <StackPanel>
                        <TextBlock Text="Availability" Style="{StaticResource textblockname}"/>
                        <StackPanel Orientation="Horizontal"  Margin="0 20 0 0">
                            <RadioButton Content="Available" IsChecked="{Binding Path=CurrentBookInDisplay.BookAvailability, Mode=TwoWay}" GroupName="Availability" FontSize="13" FontWeight="DemiBold"/>
                            <RadioButton Content="Not Available" IsChecked="{Binding CurrentBookInDisplay.BookAvailability, Mode=TwoWay, Converter={StaticResource InverseBooleanConverter}}"  GroupName="Availability" Margin="30 0 0 0" FontSize="13" FontWeight="DemiBold"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>

        <!-- Button-->
        <Border Grid.Row="3" CornerRadius="0 0 20 20" Background="LightGray">
            <StackPanel>
                <Button x:Name="update_book_btn" Command="{Binding UpdateCommand}" Content="Update Book" Style="{StaticResource loginButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
