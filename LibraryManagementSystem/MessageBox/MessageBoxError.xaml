<Window x:Class="LibraryManagementSystem.MessageBox.MessageBoxError"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryManagementSystem.MessageBox"
        xmlns:dc="clr-namespace:LibraryManagementSystem.ViewModel"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="MessageBoxError" Height="201" Width="403" WindowStyle="None" AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <dc:MemberViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="70"/>
                <RowDefinition Height="90"/>
            </Grid.RowDefinitions>

            <Border Grid.Column="0" CornerRadius="5 5 0 0" Background="#8B5DFF">
                <StackPanel>
                    <TextBlock Text="Error !" Style="{StaticResource pagetitle}" Margin="20 10 0 0" FontSize="15" Foreground="White"/>
                    <TextBlock Text="" Style="{StaticResource MessageBoxtitle}" FontSize="20" Foreground="White" Width="500" Height="41"/>
                    <Image Source="\Images\Header\close.png" Panel.ZIndex="1" x:Name="btn_close" Margin="0 -70 20 0" Style="{StaticResource closeMinimizeButton}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseUp">
                                <i:InvokeCommandAction Command="{Binding MouseUpCloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Image>
                </StackPanel>
            </Border>

            <Border Background="White" Grid.Row="1">
                <StackPanel Orientation="Vertical" VerticalAlignment="Bottom">
                    <TextBlock x:Name="megboxmsg" Text="Error Message Here !" Style="{StaticResource MessageBoxtitle}" FontSize="20" Foreground="Black" Width="396" Height="37" FontFamily="Cambria Math"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="2" Background="White" CornerRadius="0 0 5 5" Margin="0,0,0,0" Panel.ZIndex="1">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 -20 0 0">
                    <Button x:Name="add_member_btn"  Content="Ok" Style="{StaticResource MessageBoxButtonError}" Height="39" Width="112">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="Click">
                                <i:InvokeCommandAction Command="{Binding MouseUpCloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
