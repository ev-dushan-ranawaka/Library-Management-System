<Window x:Class="LibraryManagementSystem.MessageBox.MessageBoxYesNo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryManagementSystem.MessageBox"
        xmlns:dc="clr-namespace:LibraryManagementSystem.ViewModel"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="MessageBoxYesNo" Height="200" Width="400" WindowStyle="None" AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <dc:MemberViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="60"/>
            <RowDefinition Height="0*"/>
        </Grid.RowDefinitions>

        <Border Grid.Column="0" CornerRadius="5 5 0 0" Background="#8B5DFF">
            <StackPanel>
                <TextBlock Text="Confirmation" Style="{StaticResource MessageBoxtitle}" FontSize="20" Foreground="White" Width="500" Height="41"/>
                <Image Source="\Images\Header\close.png" Panel.ZIndex="1" x:Name="btn_close" Margin="0 -40 20 0" Style="{StaticResource closeMinimizeButton}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="MouseUp">
                            <i:InvokeCommandAction Command="{Binding MouseUpCloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </Image>
            </StackPanel>
        </Border>

        <Border Background="White" Grid.Row="1" Grid.RowSpan="2">
            <StackPanel Orientation="Vertical" VerticalAlignment="Top" Height="45" Margin="0,30,0,0">
                <TextBlock Text="Do you want to add new member?" Style="{StaticResource MessageBoxtitl2}" FontSize="15" Foreground="Black" Width="500" Height="23" FontFamily="Bahnschrift"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" Margin="0,75,0,30" Grid.RowSpan="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="add_member_btn"  Content="Yes" Style="{StaticResource MessageBoxButtonYes}" Height="39" Width="112"/>
                <Button x:Name="add_member"  Content="No" Style="{StaticResource MessageBoxButtonNo}" Height="39" Width="112"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="3" CornerRadius="0 0 5 5" Background="White">
            <StackPanel>
            </StackPanel>
        </Border>

    </Grid>
</Window>
