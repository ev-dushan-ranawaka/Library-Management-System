<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:LibraryManagementSystem.CustomControl"
    >


    <Style TargetType="{x:Type local:NavButton}">

        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:NavButton}">

                    <Border x:Name="back" CornerRadius="8" Padding="{TemplateBinding Padding}" Background="Transparent" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <StackPanel Width="170" Margin="5" Orientation="Horizontal" VerticalAlignment="Center">
                            <Path x:Name="icon" Height="24" Width="24" Stretch="Uniform" Fill="#dbd5ed" Data="{TemplateBinding Icon}"/>
                            <TextBlock x:Name="text" Foreground="#d0c0ff" FontSize="15" FontWeight="DemiBold" Text="{TemplateBinding Text}" VerticalAlignment="Center" Margin="30,0,5,0"/>
                        </StackPanel>
                    </Border>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" TargetName="back" Value="#563A9C"/>
                            <Setter Property="Fill" TargetName="icon" Value="white"/>
                            <Setter Property="Foreground" TargetName="text" Value="white"/>
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" TargetName="back" Value="#563A9C"/>
                            <Setter Property="Fill" TargetName="icon" Value="white"/>
                            <Setter Property="Foreground" TargetName="text" Value="white"/>
                        </Trigger>

                    </ControlTemplate.Triggers>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
